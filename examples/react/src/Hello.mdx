import {Counter} from './Counter.jsx';

# Hello

This text is written in Markdown.

We can include rich React components to the markdown: <Counter/>

Edit `Counter.jsx` or `Hello.mdx` and save to test HMR updates.
