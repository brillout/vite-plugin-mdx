{
  "name": "@brillout/vite-plugin-mdx",
  "version": "0.1.3",
  "scripts": {
    "// User Scripts": "",
    "dev": "yarn dev:prepare && yarn dev:start",
    "dev:prepare": "yarn clean:plugin && yarn install",
    "dev:start": "yarn tsc:watch",
    "build": "yarn build:prepare && yarn build:start",
    "build:prepare": "yarn clean:plugin && yarn install",
    "build:start": "yarn tsc:once",
    "// TypeScript": "",
    "tsc:watch": "tsc --watch",
    "tsc:once": "tsc",
    "// Clean": "",
    "clean": "yarn clean:all",
    "clean:plugin": "git clean -Xdf --exclude '!examples/**/*'",
    "clean:all": "git clean -Xdf"
  },
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "peerDependencies": {
    "@mdx-js/mdx": "*",
    "@mdx-js/react": "*",
    "react": "*",
    "vite": "*"
  },
  "dependencies": {
    "esbuild": "^0.8.31",
    "remark-frontmatter": "^2.0.0"
  },
  "devDependencies": {
    "@mdx-js/mdx": "^1.6.22",
    "@types/es-module-lexer": "^0.3.0",
    "@types/node": "^14.14.22",
    "typescript": "^4.1.3",
    "vite": "^2.0.0-beta.67"
  },
  "description": "Vite plugin for MDX",
  "license": "MIT",
  "repository": "https://github.com/brillout/vite-plugin-mdx",
  "keywords": [
    "vite",
    "mdx"
  ],
  "publishConfig": {
    "access": "public"
  }
}
